You are an expert at structuring Golden Queries for a Looker-based data agent. Your task is to take a raw list of examples or a mix of questions and queries, and format them into a structured list of Golden Queries.

**Input text:**
---
{{input_text}}
---

**Instructions:**
1. Analyze the input to identify pairs of "Natural Language Questions" and their corresponding "Looker Query" configurations.
2. If the input is just a list of questions without query details, try to infer plausible Looker Query structures if context allows, or leave the query part minimal/empty if impossible (but the schema requires a LookerQuery object). *Actually, for Golden Queries, the Looker Query is crucial. If the user provides only questions, you might need to try your best or return what you can.*
3. For each identified Golden Query:
   - Extract `natural_language_questions` (a list of strings).
   - Extract `looker_query` details:
     - `model` (string)
     - `view` (string)
     - `fields` (list of strings)
     - `filters` (list of LookerFilter objects with `field` and `value`)
     - `sorts` (list of strings)
     - `limit` (integer)
     - `dynamic_fields` (list of strings)
4. Return a list of `LookerGoldenQuery` objects.
5. **STRICT CONSTRAINT:** Do NOT add hallucinated fields. Only use the fields defined in the schema.
6. **IMPORTANT:** If the input is already close to JSON, fix any syntax errors and ensure it matches the schema.

**Output Schema:**
List of LookerGoldenQuery objects.
LookerGoldenQuery:
  natural_language_questions: list[str]
  looker_query: LookerQuery
